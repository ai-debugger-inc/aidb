# ------------------------------------------------------------------
# Environment template for AIDB tests
# These variables are resolved from shell environment at runtime
# Set the corresponding AIDB_* variables in your shell/environment
# ------------------------------------------------------------------

ENVIRONMENT=test
AIDB_LOG_LEVEL=DEBUG
